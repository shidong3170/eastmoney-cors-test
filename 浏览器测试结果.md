# 浏览器测试结果

## 测试上下文

- 页面：https://shidong3170.github.io/eastmoney-cors-test/
- 来源协议：HTTPS
- 自动测试浏览器：正常 Chrome 浏览器
- 浏览器安全策略：保持默认；未关闭 CORS，未借助扩展权限放宽跨域访问
- 测试时间：2026-09-11 21:42 +08:00

## fetch + CORS

- 结果：**成功**
- HTTP 状态：`200`
- Content-Type：`application/json; charset=UTF-8`
- 页面脚本读取 Access-Control-Allow-Origin：`null`
- 带 `Origin: https://shidong3170.github.io` 的原始响应头：`Access-Control-Allow-Origin: https://shidong3170.github.io`
- 返回 JSON：包含 `code: 000852`、`name: 中证1000` 和 5 条日K数据

说明：`Access-Control-Allow-Origin` 是 CORS 协议响应头，但默认不属于允许页面脚本读取的普通响应头，因此测试页中的 `response.headers.get(...)` 显示 `null`。浏览器实际完成跨源 fetch、返回 HTTP 200 并允许脚本读取 JSON，是 CORS 可用的直接证据。原始响应头仅作为补充记录。

## JSONP / script 标签

- 结果：**成功**
- 动态回调：已正常调用
- 返回 JSON：包含 `code: 000852`、`name: 中证1000` 和 5 条日K数据

## 浏览器 console

- 关键报错：无
- CORS policy / Failed to fetch：未出现
- script 加载错误：未出现

## 显示与交互核对

- “执行 fetch 测试”按钮：已通过真实点击执行。
- “执行 JSONP 测试”按钮：已通过真实点击执行。
- 两个结果区域：均显示成功状态和返回数据。
- 页面宽度：无横向滚动；长结果通过正常纵向滚动展示。
- 探索性检查：重复读取两个结果区域并检查控制台，未发现状态回退、脚本错误或结果缺失。

## 最终判定

**A．fetch 可直接使用。**

普通 HTTPS 网页/PWA 当前可考虑使用浏览器 fetch 直接访问东方财富 000852 历史行情接口。JSONP 同时可用，但在 fetch 已成功的情况下无需将 JSONP 作为首选。

## 手机实测

**已完成，结果成功。**

用户于 2026-09-11 21:50:58 +08:00 按测试流程反馈手机测试成功，记录为：

- 手机 fetch：成功
- 手机 JSONP：成功

移动端结果与桌面 Chrome 一致，最终判定维持 **A．fetch 可直接使用**，无需继续等待手机确认。
